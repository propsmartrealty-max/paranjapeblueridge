---
import Layout from '../../layouts/Layout.astro';
import { blueRidgeClusters, ClusterResidence } from '../../data/cms/clusters';
import { 
  ArrowRight, Download, Eye, Sparkles, Building2, Ruler, 
  ShieldCheck, Trees, Waves, GraduationCap, CheckCircle2, Navigation, Layers
} from 'lucide-react';

export const prerender = true;

export function getStaticPaths() {
  return blueRidgeClusters.map((cluster) => ({
    params: { cluster: cluster.slug },
    props: { cluster }
  }));
}

interface Props {
  cluster: ClusterResidence;
}

const { cluster } = Astro.props;
---

<Layout
  title={`${cluster.name} | Paranjape Blue Ridge Hinjewadi Phase 1, Pune`}
  description={`${cluster.name} at Paranjape Blue Ridge Hinjewadi Phase 1: ${cluster.configurations}, ${cluster.carpetAreaRange}, ${cluster.towerDetails.storeys} storeys. MahaRERA: ${cluster.reraNumber}.`}
  pathname={`/blue-ridge/${cluster.slug}`}
  ogImage={cluster.heroImage}
>
  <div class="bg-[#FAF9F6] text-[#070D1A] selection:bg-[#B88E3E] selection:text-white pt-24 pb-20">
    
    <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
         HERO CHAPTER
         ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
    <header class="relative min-h-[80vh] flex items-center justify-center overflow-hidden py-20 border-b border-slate-200/80 bg-gradient-to-b from-[#F3F5F8] to-[#FAF9F6]">
      <div class="absolute inset-0 z-0">
        <img
          src={cluster.heroImage}
          alt={cluster.name}
          class="w-full h-full object-cover scale-105 opacity-30 filter brightness-105"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-[#FAF9F6] via-[#FAF9F6]/80 to-transparent"></div>
        <div class="absolute inset-0 bg-gradient-to-r from-[#FAF9F6]/90 via-transparent to-[#FAF9F6]/90"></div>
      </div>

      <div class="container mx-auto px-4 sm:px-6 relative z-10 max-w-5xl text-center flex flex-col items-center">
        <div class="inline-flex items-center gap-2.5 px-4 py-1.5 rounded-full glass-pill border border-slate-200 text-[10px] font-mono uppercase tracking-[0.25em] text-[#8F6A24] mb-6 shadow-sm">
          <span>A Distinctive Residential Address Within Blue Ridge</span>
        </div>

        <span class="text-xs font-mono uppercase tracking-[0.3em] text-slate-500 mb-3 block font-medium">
          PARANJAPE BLUE RIDGE • {cluster.eyebrow}
        </span>

        <h1 class="text-4xl sm:text-7xl lg:text-8xl font-serif font-normal text-[#070D1A] tracking-tight leading-[1.04] mb-6">
          {cluster.name}
        </h1>

        <p class="text-base sm:text-xl text-slate-600 max-w-2xl mx-auto leading-relaxed font-sans font-normal mb-10">
          {cluster.tagline}
        </p>

        <div class="flex flex-wrap gap-4 justify-center">
          <a
            href="#floor-plans"
            class="btn-champagne px-8 py-3.5 rounded-full text-xs font-bold uppercase tracking-widest no-underline shadow-sm"
          >
            Explore Floor Plans
          </a>
          <a
            href="#availability"
            class="btn-glass-outline px-8 py-3.5 rounded-full text-xs font-semibold uppercase tracking-widest no-underline"
          >
            Check Availability
          </a>
        </div>

        <!-- Metric strip -->
        <div class="grid grid-cols-2 sm:grid-cols-4 gap-4 sm:gap-8 pt-12 mt-12 border-t border-slate-200/80 w-full max-w-4xl text-left">
          <div>
            <span class="text-[10px] font-mono text-slate-500 uppercase tracking-wider block">Configuration</span>
            <span class="text-sm font-semibold text-[#070D1A]">{cluster.configurations}</span>
          </div>
          <div>
            <span class="text-[10px] font-mono text-slate-500 uppercase tracking-wider block">Carpet Range</span>
            <span class="text-sm font-semibold text-[#070D1A] font-mono">{cluster.carpetAreaRange}</span>
          </div>
          <div>
            <span class="text-[10px] font-mono text-slate-500 uppercase tracking-wider block">Structure</span>
            <span class="text-sm font-semibold text-[#070D1A]">{cluster.towerDetails.storeys} Storeys</span>
          </div>
          <div>
            <span class="text-[10px] font-mono text-slate-500 uppercase tracking-wider block">MahaRERA</span>
            <span class="text-sm font-semibold text-[#B88E3E] font-mono">{cluster.reraNumber}</span>
          </div>
        </div>
      </div>
    </header>

    <div class="container mx-auto px-4 sm:px-6 max-w-6xl space-y-28 py-20">

      <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
           01 — THE RESIDENCE
           ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
      <section id="residence" class="space-y-10">
        <div class="max-w-2xl">
          <span class="text-[10px] font-mono uppercase tracking-[0.25em] text-[#8F6A24] block mb-2 font-semibold">01 — The Residence</span>
          <h2 class="text-3xl sm:text-5xl font-serif text-[#070D1A] font-normal">Architectural Concept & Layouts</h2>
        </div>

        <p class="text-base sm:text-lg text-slate-600 font-sans font-normal leading-relaxed max-w-4xl">
          {cluster.architecturalStory}
        </p>

        <!-- Concept Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {cluster.conceptPoints.map((pt, i) => (
            <div class="glass-panel p-6 rounded-3xl border border-slate-200/80 bg-white space-y-3 shadow-sm">
              <span class="text-[10px] font-mono text-[#8F6A24] uppercase tracking-widest block font-semibold">Core Design Pillar 0{i + 1}</span>
              <h3 class="text-lg font-serif text-[#070D1A] font-medium">{pt.title}</h3>
              <p class="text-xs text-slate-600 leading-relaxed font-sans font-normal">{pt.description}</p>
            </div>
          ))}
        </div>

        <!-- Specifications -->
        <div class="glass-panel rounded-3xl p-8 border border-slate-200/80 bg-white shadow-sm space-y-6">
          <h3 class="text-xl font-serif text-[#070D1A] font-medium">Fine Materiality & Specifications</h3>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6 pt-4 border-t border-slate-100">
            {cluster.specifications.map((spec) => (
              <div class="space-y-2">
                <span class="text-xs font-mono text-[#8F6A24] uppercase tracking-wider font-semibold block">{spec.category}</span>
                <ul class="space-y-1.5 text-xs text-slate-600">
                  {spec.details.map((item) => (
                    <li class="flex items-start gap-2">
                      <CheckCircle2 size={13} class="text-[#B88E3E] shrink-0 mt-0.5" />
                      <span>{item}</span>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
           02 — FLOOR PLANS (INTERACTIVE VIEWER)
           ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
      <section id="floor-plans" class="space-y-10">
        <div class="flex flex-col md:flex-row md:items-end justify-between gap-6">
          <div class="max-w-2xl">
            <span class="text-[10px] font-mono uppercase tracking-[0.25em] text-[#8F6A24] block mb-2 font-semibold">02 — Floor Plans</span>
            <h2 class="text-3xl sm:text-5xl font-serif text-[#070D1A] font-normal">Layouts & Room Dimensions</h2>
          </div>
          <a href="#enquiry" class="btn-champagne px-6 py-3 rounded-full text-xs font-bold uppercase tracking-wider no-underline shadow-sm">
            Download Complete PDF Set
          </a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {cluster.floorPlans.map((plan) => (
            <div class="glass-panel rounded-3xl p-5 border border-slate-200/80 bg-white hover:border-[#B88E3E]/50 transition-all flex flex-col justify-between shadow-sm">
              <div>
                <div class="relative h-60 rounded-2xl overflow-hidden bg-slate-50 mb-4 border border-slate-200/80">
                  <img
                    src={plan.planImage2D}
                    alt={plan.configTitle}
                    class="w-full h-full object-contain p-4"
                  />
                  <span class="absolute top-3 left-3 glass-pill px-2.5 py-0.5 rounded-full text-[9px] font-mono uppercase tracking-wider text-[#070D1A] bg-white">
                    {plan.bhkNumber} Bedroom Layout
                  </span>
                </div>

                <h3 class="text-lg font-serif text-[#070D1A] font-medium mb-1">{plan.configTitle}</h3>
                <div class="text-xs font-mono text-[#B88E3E] font-bold mb-3">{plan.carpetArea} Carpet Area</div>
                <div class="text-xs text-slate-500 font-mono pb-4 border-b border-slate-100">{plan.dimensions}</div>
              </div>

              <div class="pt-4 flex gap-2">
                <a
                  href="#enquiry"
                  class="flex-1 py-2.5 rounded-xl btn-champagne text-center text-xs font-bold uppercase tracking-wider no-underline"
                >
                  Enquire Plan
                </a>
              </div>
            </div>
          ))}
        </div>
      </section>

      <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
           03 — AVAILABILITY (DYNAMIC CMS-POWERED)
           ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
      <section id="availability" class="space-y-10">
        <div class="max-w-2xl">
          <span class="text-[10px] font-mono uppercase tracking-[0.25em] text-[#8F6A24] block mb-2 font-semibold">03 — Verified Availability</span>
          <h2 class="text-3xl sm:text-5xl font-serif text-[#070D1A] font-normal">Active Inventory Preview</h2>
        </div>

        <div class="glass-panel rounded-3xl p-6 sm:p-8 border border-slate-200/80 bg-white shadow-sm overflow-x-auto">
          <table class="w-full text-left text-xs font-sans">
            <thead>
              <tr class="border-b border-slate-200 text-[10px] font-mono uppercase tracking-wider text-slate-500">
                <th class="pb-3">Residence Type</th>
                <th class="pb-3">Carpet Area</th>
                <th class="pb-3">Floor Range</th>
                <th class="pb-3">View & Orientation</th>
                <th class="pb-3">Starting Price</th>
                <th class="pb-3 text-right">Status</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-100">
              {cluster.inventoryPreview.map((item) => (
                <tr class="hover:bg-slate-50 transition-colors">
                  <td class="py-4 font-semibold text-[#070D1A]">{item.type}</td>
                  <td class="py-4 font-mono text-slate-600">{item.carpetArea}</td>
                  <td class="py-4 text-slate-600">{item.floorRange}</td>
                  <td class="py-4 text-slate-600">{item.viewOrientation}</td>
                  <td class="py-4 font-mono text-[#B88E3E] font-semibold">{item.pricingStatus}</td>
                  <td class="py-4 text-right">
                    <span class="inline-block px-2.5 py-1 rounded-full text-[10px] font-mono uppercase tracking-wider bg-slate-100 border border-slate-200 text-slate-700">
                      {item.status}
                    </span>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
          <div class="mt-4 pt-4 border-t border-slate-100 text-[10px] font-mono text-slate-500 text-right">
            * All inventory availability subject to real-time contract execution and RERA validation.
          </div>
        </div>
      </section>

      <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
           04 & 05 — AMENITIES (CLUSTER VS. TOWNSHIP TIER SEPARATION)
           ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
      <section id="amenities" class="space-y-10">
        <div class="max-w-2xl">
          <span class="text-[10px] font-mono uppercase tracking-[0.25em] text-[#8F6A24] block mb-2 font-semibold">04 & 05 — Dual-Tier Amenities</span>
          <h2 class="text-3xl sm:text-5xl font-serif text-[#070D1A] font-normal">Cluster Privileges vs. 138-Acre Township</h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <!-- Cluster Dedicated Tier -->
          <div class="glass-panel p-8 rounded-3xl border border-slate-200/80 bg-white space-y-6 shadow-sm">
            <div class="flex items-center gap-2 text-[#8F6A24] font-mono text-xs uppercase tracking-widest border-b border-slate-100 pb-3 font-semibold">
              <Sparkles size={16} class="text-[#B88E3E]" />
              <span>Exclusive to {cluster.name} Residents</span>
            </div>
            <ul class="space-y-3 text-xs sm:text-sm text-slate-600 font-sans">
              {cluster.clusterAmenities.map((am) => (
                <li class="flex items-center gap-2.5">
                  <span class="w-1.5 h-1.5 rounded-full bg-[#B88E3E] shrink-0"></span>
                  <span>{am}</span>
                </li>
              ))}
            </ul>
          </div>

          <!-- 138-Acre Township Tier -->
          <div class="glass-panel p-8 rounded-3xl border border-slate-200/80 bg-white space-y-6 shadow-sm">
            <div class="flex items-center gap-2 text-[#8F6A24] font-mono text-xs uppercase tracking-widest border-b border-slate-100 pb-3 font-semibold">
              <Trees size={16} class="text-[#B88E3E]" />
              <span>138-Acre Blue Ridge Master Amenities</span>
            </div>
            <ul class="space-y-3 text-xs sm:text-sm text-slate-600 font-sans">
              {cluster.townshipAmenities.map((am) => (
                <li class="flex items-center gap-2.5">
                  <span class="w-1.5 h-1.5 rounded-full bg-blue-500 shrink-0"></span>
                  <span>{am}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </section>

      <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
           06 — LOCATION WITHIN BLUE RIDGE (YOU ARE HERE)
           ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
      <section id="location" class="space-y-10">
        <div class="max-w-2xl">
          <span class="text-[10px] font-mono uppercase tracking-[0.25em] text-[#8F6A24] block mb-2 font-semibold">06 — Masterplan Position</span>
          <h2 class="text-3xl sm:text-5xl font-serif text-[#070D1A] font-normal">Location Within Blue Ridge</h2>
        </div>

        <div class="glass-panel rounded-3xl p-8 border border-slate-200/80 bg-white shadow-sm space-y-6">
          <div class="flex items-center justify-between">
            <div>
              <span class="text-[10px] font-mono uppercase tracking-widest text-[#8F6A24] block font-semibold">Precinct Designation</span>
              <div class="text-xl font-serif text-[#070D1A] font-semibold mt-1">{cluster.masterplanPosition.zone}</div>
            </div>
            <div class="glass-pill px-4 py-1.5 rounded-full text-xs font-mono uppercase text-[#070D1A] font-bold bg-amber-50 border border-amber-200">
              YOU ARE HERE: {cluster.name}
            </div>
          </div>

          <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 pt-4 border-t border-slate-100">
            {cluster.masterplanPosition.nearbyKeyPoints.map((point) => (
              <div class="p-4 rounded-2xl bg-slate-50 border border-slate-200/60 flex items-center gap-3">
                <Navigation size={16} class="text-[#B88E3E] shrink-0" />
                <span class="text-xs text-slate-600 font-medium">{point}</span>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
           FINAL CONCIERGE CTA
           ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -->
      <section id="enquiry" class="py-16 text-center glass-panel rounded-3xl p-8 border border-slate-200/80 bg-white shadow-md">
        <span class="text-[10px] font-mono uppercase tracking-[0.25em] text-[#8F6A24] block mb-2 font-semibold">Private Viewing</span>
        <h3 class="text-2xl sm:text-4xl font-serif text-[#070D1A] font-normal mb-4">
          Experience {cluster.name} in Person
        </h3>
        <p class="text-slate-600 text-sm max-w-md mx-auto mb-8 font-sans font-normal">
          Reserve an exclusive guided walkthrough of sample residences, golf views, and the river promenade with our senior advisory team.
        </p>
        <a
          href="/#enquiry"
          class="btn-champagne px-8 py-3.5 rounded-full text-xs font-bold uppercase tracking-widest no-underline inline-block shadow-sm"
        >
          Book VIP Site Visit
        </a>
      </section>

    </div>
  </div>
</Layout>
